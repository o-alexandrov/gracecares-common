import * as common from "./_common"

export { regex } from "./_common"

export const name = common.setName(`V`)

export const definition = common.setDefinition({
  example: "14738",
  description: `A short numeric code that is sent to verify`,
})
