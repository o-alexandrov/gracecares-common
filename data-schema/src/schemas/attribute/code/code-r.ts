import * as common from "./_common"

export { regex } from "./_common"

export const name = common.setName(`R`)

export const definition = common.setDefinition({
  example: "14783",
  description: `A short numeric code that is sent to recover.`,
})
